Проект не собирать и не запускать.

Если при решении задачи возникают вопросы, выведи их полный список с кратким описанием (максимум 2 предложения), после чего выведи первый вопрос с конкретикой.
Переходи к следующему вопросу только после получения команды.

Все файлы проекта, включая исходники и текстовые файлы, написаны в кодировке UTF-8, новые файлы должны создаваться в этой кодировке.
При создании новых файлов, если в этом типе файлов возможно создание комментариев, в начале файла создавать комментарий "русский текст для того чтобы редакторы не путали кодировку".
При создании файлов исходи из допустимой максимальной длины строки в 200 символов.

Все комментарии в исходных файлах создавай на английском, кроме обязательного первого комментария-маркера кодировки. Не создавай комментариев, описывающих поведение кода, если это поведение очевидно из самого кода.

Все сдвиги в тексте должны быть размером в 2 пробела.

Удаляй все не используемые в коде import.

При создании или анализе комментариев несколько расположенных подряд комментариев интерпретируй как один и анализируй целиком.
Когда создаешь комментарии к классам или функцяим НЕ ПИШИ туда самоочевидные вещи, не описывай параметры смысл которых понятен из имене и типа, возвращаемые значения если они понятны из типа и описания функции.
Не создавай развернутые описание для коротких функций действие которых очевидно из тела функции. Создавай сжатое описание с общим назначением только в случае, если назначение функции непонятно из ее названия.
Комментарии из одной строки всегда создавай в формате: /** КОММЕНТАРИЙ */
Комментари для полей, констант и переменных всегда пиши после элемента в той же строке где объявлен элемент.

Не удаляй и не сокращай комментарии, которые объясняют неочевидную причину существования кода, архитектурное решение, ограничение платформы, внешнее поведение API или предотвращаемую проблему. Такие комментарии можно редактировать только при сохранении всех существенных причинно-следственных связей и ожидаемого эффекта. Если невозможно уверенно сократить комментарий без потери смысла, оставляй его без изменений.

Требование не создавать развёрнутые комментарии относится к новым комментариям. Существующие содержательные комментарии не считать избыточными только из-за длины.

Если у какого-то публичного элемента в файле окомментарий отсутствует проанализируй его назначение по этому файлу и, если назначение не очевидно из описания, создай для него комментарий.

Перед удалением или сокращением комментария проверь: отвечает ли он на вопрос «почему код сделан именно так, а не иначе?». Если да — сохраняй этот ответ в комментарии.